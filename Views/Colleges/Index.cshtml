@model List<LABA3.Models.Entities.College>

<h1>Colleges</h1>

<table class="table">
    <thead>
    <tr>
        <th>Name</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var college in Model)
    {
        <tr>
            <td>@college.Name</td>
            <td>
                <a class="btn btn-primary" asp-controller="Colleges" asp-action="Edit" asp-route-id="@college.Id">Edit</a>
            </td>
            <td>
                <a class="btn btn-primary" asp-controller="Colleges" asp-action="Details" asp-route-id="@college.Id">Details</a>
            </td>
        </tr>
    }
    </tbody>
</table>
<div class="mt-3">
    <a class="mt-3" asp-action="Create">Create New</a>
</div>