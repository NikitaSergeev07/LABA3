@model List<LABA3.Models.Entities.Course>

<h1>Courses</h1>

<table class="table">
    <thead>
    <tr>
        <th>Name</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var course in Model)
    {
        <tr>
            <td>@course.Name</td>
            <td>
                <a class="btn btn-primary" asp-controller="Courses" asp-action="Edit" asp-route-id="@course.Id">Edit</a>
            </td>
            <td>
                <a class="btn btn-primary" asp-controller="Courses" asp-action="Details" asp-route-id="@course.Id">Details</a>
            </td>
        </tr>
    }
    </tbody>
</table>
<div class="mt-3">
    <a class="mt-3" asp-action="Create">Create New</a>
</div>